<odoo>
  <record id="view_partner_form_inherit_account_responsible" model="ir.ui.view">
    <field name="name">res.partner.form.account.responsible</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
      <!-- Insertar el campo en la pestaña de contactos secundarios -->
      <xpath expr="//field[@name='website']" position="after">
        <field name="is_account_responsible"/>
      </xpath>
    </field>
  </record>
</odoo>